# Request: <one line, what you want, in your words>

<!-- The requester writes this, before Understand. Ten minutes. Any problem: an app, a repair,
     a document, a house. The Solver reads it and derives the done-checks; it can derive checks
     from features on its own, but it cannot derive what you will privately judge the result by.
     That is the one thing only you can write. Leave a section out and the Solver guesses it. -->

## Why
<two or three sentences: what is wrong today, and what "solved" feels like>

## I will judge it by
<!-- The two to five things you will actually check when it comes back. Not features; judgments.
     Each becomes a done-check, or the Solver names it in Restated with the reason it was dropped.
     Example: "I type a search term and the match appears before I finish typing." -->
- <judgment 1>
- <judgment 2>

## Wrong looks like
<!-- The failures you would notice first. The runs so far missed exactly what nobody named here:
     a route that crashed on `//`, a save that looked like success and wasn't. -->
- <a failure you would not accept>

## Must not change
<!-- What the Solver may not move: a test, a schema, a public interface, a deadline, a wall.
     Everything not listed here is assumed movable. A done-check that needs one of these moved is
     a defect in the request, found at Understand instead of at the end. -->
- <an immovable thing, by its location>

## Not this
- <what a helpful stranger might build that you do not want>

## Where it lives, who uses it
<!-- The environment a stranger runs it in (no shell of yours, no tool you installed by hand), and
     who that stranger is. A check is written for them, not for you. -->
<runtime, machine, audience>

## The one walk-through
<!-- One concrete pass, start to finish, of a person doing the main thing. This becomes D1. -->
<I open …, I do …, I see …>

## What you already know
<!-- A prior attempt, a related problem already solved, the person who knows. Optional. -->
- <…>
