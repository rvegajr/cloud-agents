# Accept: <PROBLEM title>

<!-- The requester's second moment. It happens twice: five minutes before any unit runs, on the
     done-checks; and once at the end, on the result, against those same checks. The first read is
     where being wrong is cheapest. The second is a judgment, not a test walk: the Verifier and the
     blind reviewer already did the stranger's part. Nothing in the loop reads this file yet. -->

## Before any unit runs
<!-- Read `.polya/PROBLEM.md`: Restated and the done-checks. Answer three questions. -->
- Is every line of my "I will judge it by" a D, or named in Restated with a reason it was dropped? <yes | D missing: …>
- Is there a D I would not pay for? <none | D<n>: …>
- Does any D need something in "Must not change" moved? <none | D<n> vs …>

Decision: go | edit D<n> to "…" | stop, the problem is misunderstood because …

## After look back
<!-- Read `.polya/LOOKBACK.md`: the Result table, D by D, then Judgment and Findings. -->
- D by D: does "met" mean what I meant? <D<n> met as I meant | D<n> met the letter, not the point: …>
- What did the stranger find that I never asked for? <from Findings and the blind review; one line each>
- Does it answer the problem I had, apart from the checks? <yes | no, because …>

Decision: accept | accept, follow-up on D<n> | back to the Solver, one sentence naming the D: …

Unmet and accepted anyway: <none | D<n>, because … (this becomes a Known limitation)>

## Lesson
<!-- One line for the ledger, in the requester's voice, or "none". The step from a lesson to a
     question the next Understand always asks is a judgment that it is general; make it here. -->
<L: … | none>
